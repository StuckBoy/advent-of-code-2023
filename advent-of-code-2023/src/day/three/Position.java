package day.three;

/**
 * Represents a location within the test data.
 *
 * @param row Indicates the mapped row.
 * @param position Indicates the position within the row.
 */
public record Position(Integer row, Integer position) {
}
